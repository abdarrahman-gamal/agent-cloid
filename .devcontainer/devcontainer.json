{
  "name": "OpenCode AI - GitHub Codespaces",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  
  "features": {
    "ghcr.io/devcontainers/features/node:1": {},
    "ghcr.io/devcontainers/features/desktop-lite:1": {
      "password": "codespace"
    }
  },

  // إعدادات البورت لرؤية واجهة OpenCode الرسومية
  "forwardPorts": [6080],
  "portsAttributes": {
    "6080": {
      "label": "OpenCode Desktop GUI",
      "onAutoForward": "openBrowser"
    }
  },

  // تثبيت الأداة والتطبيق بمجرد بناء الـ Codespace
  "postCreateCommand": "sudo apt-get update && sudo npm install -g opencode-ai && wget -O opencode.deb https://opencode.ai/download/stable/linux-x64-deb && sudo apt-get install -y ./opencode.deb && rm opencode.deb"
}